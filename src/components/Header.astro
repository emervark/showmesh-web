<header class="site-header">
  <div class="container nav">
    <a class="brand" href="#top" aria-label="Showmesh home"
      ><span class="mark">SM</span><span>Showmesh</span></a
    >
    <nav aria-label="Primary navigation">
      <a href="#model">Model</a><a href="#systems">Systems</a><a
        href="#interface">Interface</a
      ><a href="#deploy">Deploy</a>
    </nav>
    <a class="nav-cta" href="#contact">Early access</a>
  </div>
</header>
<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 10;
    backdrop-filter: blur(18px);
    background: rgba(7, 9, 15, 0.72);
    border-bottom: 1px solid var(--line);
  }
  .nav {
    min-height: 72px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
  }
  .brand {
    display: inline-flex;
    align-items: center;
    gap: 12px;
    font-weight: 750;
    letter-spacing: -0.02em;
  }
  .mark {
    display: grid;
    place-items: center;
    width: 34px;
    height: 34px;
    border-radius: 10px;
    background: var(--accent);
    color: #101400;
    font-family: var(--mono);
    font-size: 0.8rem;
  }
  nav {
    display: flex;
    gap: 22px;
    color: var(--muted);
    font-size: 0.94rem;
  }
  nav a:hover {
    color: var(--text);
  }
  .nav-cta {
    border: 1px solid var(--line-strong);
    border-radius: 999px;
    padding: 9px 14px;
    color: var(--text);
    font-weight: 650;
  }
  @media (max-width: 760px) {
    nav {
      display: none;
    }
    .nav {
      min-height: 64px;
    }
  }
</style>
